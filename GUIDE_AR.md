# 🏥 CareFlow - دليل المستخدم الشامل

## 📋 نظرة عامة

**CareFlow** هو نظام إدارة رعاية صحية إلكتروني (EHR) يربط بين المرضى والأطباء والممرضات والسكرتيرات والإداريين.

---

## 🔐 المصادقة والتسجيل

### تسجيل حساب جديد (للمرضى فقط)
1. اذهب إلى `/register`
2. أدخل المعلومات المطلوبة:
   - **الاسم الكامل**: 3 أحرف على الأقل
   - **البريد الإلكتروني**: بريد إلكتروني صحيح
   - **كلمة المرور**: 8 أحرف على الأقل، تحتوي على:
     - حرف كبير (A-Z)
     - حرف صغير (a-z)
     - رقم (0-9)
   - **تأكيد كلمة المرور**: يجب أن تطابق كلمة المرور

### تسجيل الدخول
1. اذهب إلى `/login`
2. أدخل البريد الإلكتروني وكلمة المرور
3. سيتم توجيهك تلقائياً حسب دورك

### استعادة كلمة المرور
1. اذهب إلى `/forgot-password`
2. أدخل بريدك الإلكتروني
3. ستصلك رسالة بتعليمات إعادة تعيين كلمة المرور

---

## 👥 الأدوار والصلاحيات

### 1. 👤 **المريض (Patient)**

#### الصفحة الرئيسية (`/dashboard`)
- عرض المواعيد القادمة
- عرض الوصفات الطبية الأخيرة
- إحصائيات سريعة

#### المواعيد (`/my-appointments`)
- عرض جميع المواعيد (القادمة والسابقة)
- حالة الموعد: Pending, Confirmed, Completed, Cancelled
- إلغاء المواعيد

#### حجز موعد جديد (`/appointments/new`)
1. اختر طبيباً من القائمة
2. اختر التاريخ
3. اختر الوقت المتاح
4. أدخل سبب الزيارة
5. احجز الموعد

#### الوصفات الطبية (`/prescriptions`)
- عرض جميع الوصفات الطبية
- تفاصيل الأدوية (الاسم، الجرعة، المدة)
- تحميل الوصفة كـ PDF

---

### 2. 👨‍⚕️ **الطبيب (Doctor)**

#### الصفحة الرئيسية (`/dashboard`)
- عرض المواعيد اليومية
- إحصائيات المرضى
- المهام المعلقة

#### مواعيدي (`/doctor/appointments/me`)
- عرض جميع المواعيد
- تحديث حالة الموعد:
  - `pending` → `confirmed` (تأكيد)
  - `confirmed` → `completed` (إكمال)
  - أي حالة → `cancelled` (إلغاء)

#### مرضاي (`/patients`)
- عرض قائمة المرضى
- البحث عن مريض
- عرض تفاصيل المريض

#### تفاصيل المريض (`/patients/:id`)
- السجل الطبي الكامل
- الزيارات السابقة
- التشخيصات والأعراض
- الوصفات الطبية
- إضافة زيارة جديدة

#### إضافة زيارة طبية
```javascript
{
  diagnosis: ["التشخيص"],
  symptoms: ["الأعراض"],
  treatments: [
    {
      name: "اسم الدواء",
      dosage: "الجرعة",
      duration: "المدة"
    }
  ],
  notes: "ملاحظات إضافية"
}
```

---

### 3. 👩‍⚕️ **الممرضة (Nurse)**

#### الصفحة الرئيسية (`/dashboard`)
- عرض المواعيد المخصصة
- متابعة حالة المرضى

#### مواعيدي (`/nurse/appointments/me`)
- عرض المواعيد التي تحتاج متابعة
- تفاصيل كل موعد

---

### 4. 📋 **السكرتيرة (Secretary)**

#### الصفحة الرئيسية (`/dashboard`)
- البحث عن المرضى
- عرض جميع المواعيد
- حجز مواعيد للمرضى

#### البحث عن مرضى (`/secretary/patients`)
- البحث بالاسم أو البريد الإلكتروني
- عرض نتائج البحث
- حجز موعد للمريض

#### جميع المواعيد (`/secretary/appointments`)
- عرض جميع المواعيد في النظام
- Pagination (10 مواعيد لكل صفحة)
- تصفية وبحث

---

### 5. 👑 **المسؤول (Admin)**

#### الصفحة الرئيسية (`/dashboard`)
- إحصائيات النظام الكاملة
- عدد المستخدمين
- عدد المواعيد
- نشاط النظام

#### إدارة المستخدمين (`/users`)
- عرض جميع المستخدمين
- إضافة مستخدم جديد:
  - الاسم الكامل
  - البريد الإلكتروني
  - الدور (Doctor, Nurse, Secretary)
  - بيانات Profile (مثل التخصص للأطباء)
- Pagination

#### إدارة الأدوار (`/admin/roles`)
- عرض جميع الأدوار
- إنشاء دور جديد
- تحديد الصلاحيات

---

## 🔌 **APIs المستخدمة**

### Auth APIs
```
POST /auth/register        - تسجيل مريض جديد
POST /auth/login           - تسجيل الدخول
POST /auth/refresh         - تحديث التوكن
POST /auth/logout          - تسجيل الخروج
```

### Public APIs
```
GET /home/doctors                              - قائمة الأطباء
GET /home/doctors/:id/availability?date=YYYY-MM-DD  - توفر الطبيب
```

### Patient APIs
```
GET  /patient/appointments                     - مواعيدي
GET  /patient/appointments/available?doctorId=X&date=Y  - الأوقات المتاحة
POST /patient/appointments                     - حجز موعد
PATCH /patient/appointments/:id/cancel         - إلغاء موعد
GET  /patient/record/me                        - سجلي الطبي
GET  /patient/notifications/me                 - إشعاراتي
```

### Doctor APIs
```
GET  /doctor/appointments/me                   - مواعيدي
PATCH /doctor/appointments/:id/status          - تحديث حالة الموعد
GET  /doctor/patients/:id/record               - سجل المريض
POST /doctor/patients/:id/visits               - إضافة زيارة
```

### Nurse APIs
```
GET /nurse/appointments/me                     - مواعيدي
```

### Secretary APIs
```
GET  /secretary/patients?q=search              - البحث عن مرضى
GET  /secretary/appointments?page=1&perPage=10 - جميع المواعيد
POST /secretary/appointments                   - حجز موعد لمريض
```

### Admin APIs
```
GET  /admin/users?page=1&perPage=10            - جميع المستخدمين
POST /admin/users                              - إنشاء مستخدم
GET  /admin/roles                              - جميع الأدوار
POST /admin/roles                              - إنشاء دور
```

### User APIs (مشترك)
```
GET /user/me                                   - معلوماتي
GET /user/me/notifications                     - إشعاراتي
```

---

## 🎨 **المكونات القابلة لإعادة الاستخدام**

### Common Components
- **Button** - أزرار بأشكال مختلفة (primary, secondary, danger, success, outline)
- **Input** - حقول إدخال مع تسميات وأخطاء
- **Modal** - نوافذ منبثقة
- **Card** - بطاقات للمحتوى
- **Badge** - شارات ملونة للحالات
- **Alert** - تنبيهات ورسائل (success, error, warning, info)
- **LoadingSpinner** - مؤشر التحميل

### Feature Components
- **DoctorCard** - بطاقة عرض معلومات الطبيب
- **PatientCard** - بطاقة عرض معلومات المريض
- **AppointmentTable** - جدول المواعيد
- **StatCard** - بطاقة إحصائيات

---

## 🛠️ **Utils (الدوال المساعدة)**

### formatDate.js
```javascript
formatDate(date)          // "January 25, 2025"
formatTime(date)          // "10:30 AM"
formatDateTime(date)      // "Jan 25, 2025, 10:30 AM"
getRelativeTime(date)     // "2 hours ago"
```

### roleHelpers.js
```javascript
isAdmin(user)             // true/false
isDoctor(user)            // true/false
isPatient(user)           // true/false
isNurse(user)             // true/false
isSecretary(user)         // true/false
isMedicalStaff(user)      // Doctor or Nurse
canAccessPatientRecords(user)  // Admin or Medical Staff
getRoleBadgeColor(role)   // Tailwind color class
```

---

## 🚀 **كيفية التشغيل**

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إعداد ملف .env
```
VITE_API_URL=http://localhost:5000/api
```

### 3. تشغيل المشروع
```bash
npm run dev
```

### 4. فتح المتصفح
```
http://localhost:5173
```

---

## 🔑 **حسابات التجربة**

### Admin
```
Email: admin@careflow.local
Password: admin123
```

### Doctor
```
Email: doctor1@careflow.local
Password: Staff123!
```

### Patient
```
Email: patient1@careflow.local
Password: Patient123!
```

### Nurse
```
Email: nurse1@careflow.local
Password: Staff123!
```

### Secretary
```
Email: secretary1@careflow.local
Password: Staff123!
```

---

## ✅ **الميزات المنفذة**

- ✅ نظام مصادقة كامل (Login, Register, Forgot Password)
- ✅ التحقق من صحة البيانات (Client-side validation)
- ✅ رسائل الأخطاء والنجاح (Alert component)
- ✅ 5 أدوار مختلفة (Admin, Doctor, Patient, Nurse, Secretary)
- ✅ لوحات تحكم مخصصة لكل دور
- ✅ إدارة المواعيد الكاملة
- ✅ السجلات الطبية
- ✅ الوصفات الطبية
- ✅ إدارة المستخدمين (Admin)
- ✅ مكونات قابلة لإعادة الاستخدام
- ✅ تصميم احترافي وجميل
- ✅ Responsive Design

---

## 📝 **ملاحظات مهمة**

1. **التوكن**: يتم حفظ `accessToken` في `localStorage`
2. **الحماية**: جميع المسارات محمية حسب الدور
3. **الأخطاء**: يتم عرض رسائل الأخطاء بشكل واضح
4. **التحميل**: Loading states في جميع العمليات
5. **التحقق**: Client-side validation قبل إرسال البيانات

---

## 🎯 **الخطوات التالية (اختياري)**

- [ ] إضافة Real-time notifications
- [ ] إضافة Chat بين الطبيب والمريض
- [ ] إضافة تحميل الملفات
- [ ] إضافة التقارير والإحصائيات المتقدمة
- [ ] إضافة نظام الدفع
- [ ] إضافة Multi-language support

---

**تم بناء المشروع بواسطة: Antigravity AI** 🚀
**التاريخ: 19 نوفمبر 2025**
